import { TransformedPropertiesPipe } from './transformed-properties.pipe';

describe('TransformedPropertiesPipe', () => {
  it('create an instance', () => {
    const pipe = new TransformedPropertiesPipe();
    expect(pipe).toBeTruthy();
  });
});
