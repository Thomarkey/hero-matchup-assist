import { HeroImagesPipe } from './hero-images.pipe';

describe('HeroImagesPipe', () => {
  it('create an instance', () => {
    const pipe = new HeroImagesPipe();
    expect(pipe).toBeTruthy();
  });
});
